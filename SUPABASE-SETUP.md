# เชื่อม LittleApp กับ Supabase Free

## 1. สร้างตารางและสิทธิ์ข้อมูล

ในโปรเจกต์ Supabase เปิด **SQL Editor → New query** คัดลอกข้อความทั้งหมดจาก `supabase/schema.sql` วางแล้วกด **Run**

สคริปต์นี้สร้างตาราง `littleapp_journals` เก็บรายการเทรดและรายการสะสมเป็น JSON หนึ่งชุดต่อบัญชี โดยยังคงรูปแบบไฟล์สำรองเดิม เปิด Row Level Security และอนุญาตให้อ่านเฉพาะข้อมูลของเจ้าของบัญชี การเขียนผ่านฟังก์ชัน `littleapp_save` จะตรวจบัญชีและเลข revision ป้องกันการเขียนทับจากข้อมูลเก่า

## 2. กำหนดค่าเชื่อมต่อ

เปิด `dist/config.js` ใส่ค่าจากโปรเจกต์ของคุณ:

```js
window.LITTLEAPP_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabasePublishableKey: 'YOUR_PUBLISHABLE_KEY'
};
```

Project URL ดูได้จากปุ่ม **Connect** ของโปรเจกต์ และ Publishable key ดูได้ใน **Settings → API Keys** ชื่อเมนูอาจต่างกันเล็กน้อยตามหน้า Dashboard ถ้าเป็นโปรเจกต์เก่าใช้ `anon` key ได้

คีย์ประเภทนี้เป็นคีย์หน้าเว็บที่เผยแพร่ได้เมื่อใช้คู่กับสิทธิ์ข้อมูลตามสคริปต์ ห้ามใช้ Secret key, `service_role` หรือรหัสผ่านฐานข้อมูล

## 3. เตรียมบัญชีเข้าใช้งาน

สำหรับใช้คนเดียว: เปิด **Authentication → Users → Add user → Create new user** ใส่อีเมลและรหัสผ่านของคุณ และเลือกยืนยันอีเมลอัตโนมัติถ้ามีตัวเลือก จากนั้นใช้บัญชีนี้เข้าสู่ LittleApp ได้เลย วิธีนี้ไม่ต้องตั้งค่าระบบส่งอีเมล

ถ้าต้องการสมัครจากหน้าเว็บ: เปิด Email provider และอนุญาตการสมัครบัญชีใน Authentication ถ้าเปิด Confirm email ผู้ใช้ต้องยืนยันอีเมลก่อนล็อกอิน บริการส่งอีเมลเริ่มต้นของ Supabase มีข้อจำกัดด้านผู้รับและจำนวนส่ง หากส่งไม่ได้ให้ใช้การสร้างผู้ใช้จาก Dashboard สำหรับบัญชีส่วนตัว หรือตั้ง SMTP ของคุณเอง

ตั้ง **Authentication → URL Configuration → Site URL** ให้เป็น URL จริงของ LittleApp บน Vercel เพื่อให้ลิงก์ยืนยันอีเมลกลับมาที่เว็บถูกต้อง

รุ่นนี้มีล็อกอิน สมัครบัญชี และออกจากระบบ ยังไม่มีหน้ากู้รหัสผ่าน ถ้าลืมรหัสผ่านให้จัดการผ่าน Supabase Dashboard ของเจ้าของโปรเจกต์

## 4. อัปเดต GitHub และ Vercel

อัปโหลดชุดไฟล์ใหม่ทั้งหมดที่ระดับบนสุดของ repository โดยแทนที่ไฟล์เดิม รวมโฟลเดอร์ `dist` และ `supabase` พร้อม `vercel.json` จากนั้น commit เพื่อให้ Vercel deploy

ตั้งค่า Vercel เหมือนเดิม: Framework **Other**, Root Directory `./`, Output Directory `dist`, ไม่ต้องใช้ Build Command, Install Command หรือ Environment Variables เนื่องจากเป็น HTML/JS แบบ static ให้ตั้งค่าสาธารณะใน `dist/config.js` โดยตรง

## 5. ตรวจสอบด้วยข้อมูลทดลอง

1. เปิดเว็บ Vercel เข้าสู่ระบบด้วยบัญชีที่สร้างไว้
2. ต้องเห็นสถานะ “โหลดข้อมูลแล้ว” ก่อนเพิ่มรายการ หากโหลดไม่สำเร็จให้ตรวจว่า Run SQL แล้ว
3. เพิ่มรายการทดสอบหนึ่งรายการ และรอ “บันทึกออนไลน์แล้ว”
4. รีเฟรชหน้า หรือเปิดจากอุปกรณ์อื่นแล้วล็อกอินบัญชีเดิม รายการต้องยังอยู่
5. ทดลองแก้ไขและลบรายการทดสอบ
6. ถ้าสร้างบัญชีที่สอง บัญชีนั้นต้องไม่เห็นรายการของบัญชีแรก

กด “โหลดข้อมูลล่าสุด” เพื่อรับการแก้ไขจากอุปกรณ์อื่น รุ่นนี้โหลดเมื่อเข้าสู่ระบบ/รีเฟรช/กดปุ่ม ไม่ใช่ realtime หากมีคนแก้ไขข้อมูลก่อนหน้าคุณ ระบบจะปฏิเสธการบันทึกที่ใช้ revision เก่า ให้คัดลอกข้อความที่ยังไม่บันทึก ปิดฟอร์ม โหลดข้อมูลล่าสุด แล้วแก้ไขอีกครั้ง

หากอินเทอร์เน็ตขาดระหว่างบันทึก ระบบจะไม่แจ้งว่าสำเร็จ ให้โหลดข้อมูลล่าสุดเพื่อตรวจสอบก่อนบันทึกซ้ำ รุ่นนี้ไม่รับการแก้ไขแบบออฟไลน์

## 6. ย้ายข้อมูลจากเวอร์ชันเดิม

ก่อนอัปเดตเว็บ กด “สำรองข้อมูล” ใน LittleApp เวอร์ชันเดิม เก็บ JSON ไว้ในเครื่อง จากนั้นเข้าสู่ระบบเวอร์ชันใหม่แล้วกด “นำเข้าข้อมูล” เลือกไฟล์นั้นและยืนยันการแทนที่ข้อมูลบัญชี

ข้อมูลที่เคยอยู่ใน localStorage ไม่ถูกลบโดยรุ่นนี้ และไม่อัปโหลดอัตโนมัติ หากยังไม่ได้สำรอง สามารถใช้เว็บเวอร์ชันเดิมบน URL/เบราว์เซอร์เดิมเพื่อส่งออกก่อน

## ขอบเขตและค่าใช้จ่าย

ใช้ Supabase แผน Free ได้ภายในโควตาของผู้ให้บริการ โปรเจกต์อาจถูกพักเมื่อใช้งานน้อย ให้เปิดกลับจาก Dashboard หากจำเป็น ไม่ต้องเพิ่มบริการเสียเงินเพื่อใช้ฟังก์ชันพื้นฐานนี้

ข้อมูลสมุดบันทึกเก็บออนไลน์ ส่วน session สำหรับล็อกอินเก็บในเบราว์เซอร์ ควรออกจากระบบเมื่อใช้เครื่องร่วมกัน สำรอง JSON เป็นระยะ

ตารางเก็บสมุดบันทึกทั้งชุด ไม่ใช่ตารางแยกต่อธุรกรรม เหมาะกับพอร์ตส่วนตัว จำกัด 10,000 รายการรวมและ payload ไม่เกิน 10 MB ต่อบัญชี การเติบโตของหลายบัญชีต้องคำนึงถึงโควตาฐานข้อมูลรวม

โค้ดและสถานการณ์สำเร็จ/ล้มเหลวทดสอบในเครื่องด้วยระบบจำลองแล้ว แต่ต้องทำขั้นตอนที่ 5 กับโปรเจกต์จริงเพื่อยืนยันการเชื่อมต่อและสิทธิ์ RLS ก่อนใช้ข้อมูลจริง
