# LittleApp v3.1 — แผนและบันทึกการเทรดในหน้าเดียว

## อัปเดต

สำรอง JSON จากเว็บเดิม แล้วอัปโหลดเนื้อหา ZIP ทั้งชุดแทนที่ไฟล์ใน GitHub รอ Vercel Ready และรีเฟรชเว็บทุกอุปกรณ์ ไม่ต้องรัน SQL ใหม่

มีไฟล์ใหม่ `dist/journal-core.js` และ `dist/journal.js` ต้องอัปโหลดพร้อม `index.html` และไฟล์อื่นในชุด อย่าอัปโหลดเฉพาะไฟล์ใหม่

## วิธีใช้

เมนู “แผนก่อนเทรด” ถูกรวมเข้ากับ “บันทึกการเทรด” ใช้ปุ่ม “เพิ่มรายการ” ปุ่มเดียว:

1. **รอเข้า** — กรอกแผน ราคาเข้า SL/TP วงเงินขาดทุน และขั้นจำนวน ระบบคำนวณจำนวนตามแผน แต่ยังไม่นับเป็นการเทรดจริง
2. **เปิดอยู่** — เปิดรายการเดิมแล้วเปลี่ยนสถานะ กรอกวัน ราคาเข้า จำนวน ตัวคูณ และค่าธรรมเนียมจริง ไม่ต้องกรอกแผนซ้ำ
3. **ปิดแล้ว** — เปิดรายการเดิมอีกครั้ง ใส่ราคาและวันที่ออกจริง ระบบคำนวณกำไรสุทธิและใช้ในรายงานรายเดือน
4. **ยกเลิก** — ใช้กับแผนที่ยังไม่มีรายการจริง ไม่รวมในกำไรขาดทุน

ถ้าต้องการจดผลจริงโดยไม่ได้วางแผน ให้เลือก “เปิดอยู่” หรือ “ปิดแล้ว” และเอาเครื่องหมายเพิ่มแผนออก แผนที่เชื่อมแล้วจะเก็บไว้เพื่อเทียบกับผลจริง

จำนวนตามแผนกับจำนวนจริงแยกกัน กด “ใช้จำนวนตามแผนกับรายการจริง” เมื่อต้องการคัดลอก แล้วตรวจจำนวนอีกครั้งก่อนบันทึก ราคาเข้าแผน ค่าเผื่อ และเป้ากำไรเป็นเพียงประมาณการ ไม่ถูกนับเป็นผลจริง

รายการที่เปิดจริงแล้วไม่สามารถเปลี่ยนกลับเป็นรอเข้าหรือยกเลิกได้ ใช้เปิดอยู่/ปิดแล้วเพื่อรักษาประวัติ หรือเลือกการลบรายการอย่างชัดเจน หากลบรายการจริงพร้อมแผนที่เชื่อม ระบบจะลบทั้งคู่

## แผนกับรายการเดิมที่กรอกแยกไว้

ระบบไม่เดาว่าแผนใดเป็นรายการเดียวกับการเทรดใด เพื่อไม่ให้ข้อมูลถูกรวมผิด:

1. ในบันทึกการเทรด กดแก้ไขรายการ **เปิดอยู่ / ปิดแล้ว** ที่มีอยู่แล้ว
2. เลือก “เชื่อมแผนเดิมที่เคยกรอกแยกไว้”
3. เลือกแผนที่มีพอร์ต สัญญา และทิศทางตรงกัน ตรวจข้อมูลแล้วบันทึก

หลังเชื่อมจะแสดงเป็นแถวเดียว และไม่สร้างการเทรดจริงเพิ่ม แผนหนึ่งเชื่อมได้หนึ่งรายการจริง

แผนเก่าที่ตั้งสถานะ “เข้าแล้ว” แต่ยังไม่มีรายการจริงเชื่อม จะอยู่ในกลุ่มรอเข้า พร้อมข้อความอธิบายว่าเป็นแผนเดิมยังไม่เชื่อมผลจริง ถ้ามีรายการจริงอยู่แล้วให้เชื่อมจากรายการนั้น ไม่เปิดจริงซ้ำจากแผน

## การคำนวณและข้อมูล

- รายงานและปฏิทินยังนับเฉพาะ Futures ที่ปิดจริงและ Spot ที่ขายแล้ว ไม่รวมกำไรคาดหวังของแผน
- SL/TP ใช้คำนวณแผน ไม่ได้ส่งคำสั่งให้โบรกเกอร์หรือปิดสถานะอัตโนมัติ
- ข้อมูลพอร์ต ประวัติซื้อขาย รายงาน และสมุดทบทวนยังอยู่เหมือนเดิม
- ใช้ตาราง Supabase เดิม บันทึกการเชื่อมแผนและรายการจริงในครั้งเดียว พร้อมตรวจ revision ป้องกันข้อมูลทับกัน
- ทดสอบการเปลี่ยนสถานะ การแก้ไขซ้ำ การเชื่อมแผนเก่า และการไม่คิดกำไรซ้ำในระบบจำลองแล้ว ยังไม่ได้ตรวจหน้าจอหรือบันทึก v3.1 ในเว็บจริง
